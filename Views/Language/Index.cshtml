@model IEnumerable<LearningPlatform.Models.Language>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.language_name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.country_of_origin)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.difficulty_level)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.language_name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.country_of_origin)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.difficulty_level)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.language_id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.language_id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.language_id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
